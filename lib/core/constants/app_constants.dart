class AppConstants {
  // Animation durations
  static const animationDuration = Duration(milliseconds: 500);
  static const confettiDuration = Duration(seconds: 1);

  // Sizes
  static const double iconSize = 80;
  static const double buttonPadding = 20;
  static const double borderRadius = 15;
  static const double progressBarHeight = 10;

  // Scores
  static const int correctAnswerScore = 50;
}
