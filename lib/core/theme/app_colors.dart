import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const secondary = Colors.purple;
  static const accent = Colors.amber;
  static const error = Colors.red;
  static const success = Colors.green;
  static const white = Colors.white;
  static const black87 = Colors.black87;

  static final gradientStart = Colors.blue.shade900;
  static final gradientEnd = Colors.purple.shade900;

  static const overlayLight = Color.fromRGBO(255, 255, 255, 0.1);
  static const overlayMedium = Color.fromRGBO(255, 255, 255, 0.2);
}
